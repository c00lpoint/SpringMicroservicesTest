server:
  port: 8082
spring:
  application:
    name: movie-data-service
logging:
  level:
    com:
      c00lpoint:
        test:
          moviedataservice: DEBUG
#  path: ./logs
  file: ./logs/movie-data-service.log
  pattern:
    file: '%date{yyyy-MM-dd HH:mm:ss.SSSS} %-5level [%thread] %logger{15} - %msg%n'
    console: '%highlight{%date{yyyy-MM-dd HH:mm:ss.SSSS} %-5level [%thread] %logger{15} - %msg%n}'
